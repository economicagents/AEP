{
  "name": "@aep/benchmark",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "Benchmark harness: AEP-resolved vs naive x402 procurement",
  "type": "module",
  "main": "dist/run.js",
  "scripts": {
    "build": "tsc",
    "run": "node dist/run.js",
    "benchmark": "pnpm run build && pnpm run run",
    "test": "echo 'No tests'"
  },
  "dependencies": {
    "@aep/indexer": "file:../indexer",
    "@aep/resolver": "file:../resolver",
    "@aep/sdk": "file:../sdk",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "files": [
    "dist"
  ]
}
